<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Nhập Giáo Viên - Hệ thống quản lý học sinh</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/icons/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #f8fafc;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .login-container {
            max-width: 450px;
            width: 100%;
            margin: 2rem auto;
        }
        .card {
            border: none;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .card-header {
            background: #15803d;
            color: white;
            border-radius: 10px 10px 0 0 !important;
        }
        .btn-primary {
            background-color: #15803d;
            border: none;
        }
        .btn-primary:hover {
            background-color: #166534;
        }
        .logo {
            width: 80px;
            margin-bottom: 1rem;
        }
        .main-content {
            flex: 1;
        }
    </style>
</head>
<body>
    <div class="container py-3">
        <header class="pb-3 mb-4 text-center">
            <a href="/" class="d-flex align-items-center justify-content-center text-decoration-none">
                <i class="bi bi-book me-2 text-success" style="font-size: 2rem;"></i>
                <span class="fs-4 fw-semibold text-success">Hệ thống Quản lý Học sinh</span>
            </a>
        </header>
    </div>

    <div class="container main-content">
        <div class="login-container">
            <div class="card shadow border-0 rounded-3">
                <div class="card-body p-4">
                    <div class="text-center">
                        <h1 class="card-title h3 mb-4">Đăng nhập Giáo viên</h1>
                    </div>
                    
                    <% if (errorMessage) { %>
                    <div class="alert alert-danger">
                        <%= errorMessage %>
                    </div>
                    <% } %>
                    
                    <form action="/auth/teacher-login" method="POST">
                        <div class="mb-3">
                            <label for="username" class="form-label">Email</label>
                            <input type="email" class="form-control" id="username" name="username" placeholder="Nhập email" required>
                        </div>
                        
                        <div class="mb-4">
                            <label for="password" class="form-label">Mật khẩu</label>
                            <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" required>
                        </div>
                        
                        <input type="hidden" name="userType" value="teacher">
                        
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary">Đăng nhập</button>
                        </div>
                        
                        <div class="text-center mt-3">
                            <a href="#" class="small text-decoration-none">Quên mật khẩu?</a>
                            <p class="small mt-2">Bạn là quản trị viên? <a href="/auth/login" class="text-decoration-none">Đăng nhập ở đây</a></p>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <footer class="py-3 mt-4 text-center">
        <p class="text-muted small">&copy; <%= new Date().getFullYear() %> Hệ thống Quản lý Học sinh</p>
    </footer>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html> 