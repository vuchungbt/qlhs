<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Dashboard</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/icons/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('./partials/header') %>

    <!-- Main Content -->
    <div class="container-fluid">
        <!-- Welcome Section -->
        <div class="welcome-section mb-4">
            <h1 class="display-5 fw-bold">👋 Welcome, <%= username %>!</h1>
            <p class="fs-5">Hôm nay là <%= currentDate.toLocaleDateString('vi-VN', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
            <div class="mt-3">
                <a href="/academic/schedule" class="btn btn-light btn-lg">
                    <i class="bi bi-calendar-check me-2"></i>
                    Xem lịch học hôm nay
                </a>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="row mb-4">
            <div class="col-md-3 mb-3">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h5 class="card-title mb-0">Học sinh</h5>
                            <div class="icon-bg bg-light-primary rounded-circle p-2">
                                <i class="bi bi-mortarboard text-primary fs-4"></i>
                            </div>
                        </div>
                        <h2 class="display-6 fw-bold"><%= stats.studentCount || 0 %></h2>
                        <p class="text-muted mb-0">Tổng số học sinh đang học</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h5 class="card-title mb-0">Phụ huynh</h5>
                            <div class="icon-bg bg-light-warning rounded-circle p-2">
                                <i class="bi bi-people text-warning fs-4"></i>
                            </div>
                        </div>
                        <h2 class="display-6 fw-bold"><%= stats.parentCount || 0 %></h2>
                        <p class="text-muted mb-0">Phụ huynh đã đăng ký</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h5 class="card-title mb-0">Lớp học</h5>
                            <div class="icon-bg bg-light-success rounded-circle p-2">
                                <i class="bi bi-book text-success fs-4"></i>
                            </div>
                        </div>
                        <h2 class="display-6 fw-bold"><%= stats.classCount || 0 %></h2>
                        <p class="text-muted mb-0">Số lớp đang hoạt động</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h5 class="card-title mb-0">Giáo viên</h5>
                            <div class="icon-bg bg-light-info rounded-circle p-2">
                                <i class="bi bi-person-badge text-info fs-4"></i>
                            </div>
                        </div>
                        <h2 class="display-6 fw-bold"><%= stats.teacherCount || 0 %></h2>
                        <p class="text-muted mb-0">Giáo viên đang giảng dạy</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Class Schedule Section -->
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="mb-0">
                <i class="bi bi-journal-text me-2"></i>
                Lịch học của tôi
            </h2>
            <a href="/academic/schedule" class="text-primary text-decoration-none">Xem tất cả →</a>
        </div>

        <div class="row">
            <% if (courses && courses.length > 0) { %>
                <% courses.forEach(function(course, index) { 
                    let cardClasses = ['primary', 'secondary', 'tertiary', 'primary'];
                    let cardClass = cardClasses[index % 4];
                %>
                    <div class="col-md-3 mb-4">
                        <div class="class-card <%= cardClass %> course-card">
                            <h5 class="mb-3"><%= course.name %></h5>
                            <div class="teacher-name">
                                <i class="bi bi-person-circle"></i>
                                <span><%= course.teacher %></span>
                            </div>
                            <div class="class-schedule">
                                <i class="bi bi-calendar-event"></i>
                                <span><%= course.schedule %></span>
                            </div>
                            <div class="class-time">
                                <i class="bi bi-clock"></i>
                                <span><%= course.time %></span>
                            </div>
                            <div class="text-end mt-3">
                                <a href="/academic/schedule/<%= course.id %>" class="btn btn-sm btn-light">
                                    <i class="bi bi-eye me-1"></i> Chi tiết
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12">
                    <div class="alert alert-info">
                        Chưa có lớp học nào được tạo. <a href="/academic/schedule" class="alert-link">Tạo lớp học mới</a>
                    </div>
                </div>
            <% } %>
        </div>

        <!-- Recent Activity Section -->
        <div class="card mt-4">
            <div class="card-header bg-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="bi bi-activity me-2"></i>
                    Hoạt động gần đây
                </h5>
            </div>
            <div class="card-body p-0">
                <% if (recentActivities && recentActivities.length > 0) { %>
                    <ul class="list-group list-group-flush">
                        <% recentActivities.forEach(function(activity) { %>
                            <li class="list-group-item d-flex align-items-center py-3">
                                <% 
                                    let bgClass = 'bg-light-primary';
                                    if (activity.type === 'student_added') bgClass = 'bg-light-info';
                                    else if (activity.type === 'parent_added') bgClass = 'bg-light-success';
                                %>
                                <div class="activity-icon <%= bgClass %> rounded-circle p-2 me-3">
                                    <i class="bi bi-<%= activity.icon %>"></i>
                                </div>
                                <div>
                                    <p class="mb-0"><%= activity.message %></p>
                                    <small class="text-muted"><%= activity.date %></small>
                                </div>
                            </li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <div class="p-4 text-center">
                        <p class="text-muted">Chưa có hoạt động nào gần đây</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <style>
        .bg-light-primary {
            background-color: #e0e7ff;
        }
        
        .bg-light-success {
            background-color: #dcfce7;
        }
        
        .bg-light-info {
            background-color: #dbeafe;
        }
        
        .bg-light-warning {
            background-color: #fef3c7;
        }
        
        .text-primary {
            color: #4f46e5 !important;
        }
        
        .text-success {
            color: #10b981 !important;
        }
        
        .text-info {
            color: #3b82f6 !important;
        }
        
        .text-warning {
            color: #f59e0b !important;
        }
        
        .icon-bg, .activity-icon {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .activity-icon {
            width: 36px;
            height: 36px;
        }
    </style>

    <%- include('./partials/footer') %>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        // Đánh dấu menu dashboard là active
        document.addEventListener('DOMContentLoaded', function() {
            const homeLink = document.querySelector('.sidebar .nav-link[href="/"]');
            if (homeLink) {
                homeLink.classList.add('active');
            }
        });
    </script>
</body>
</html>