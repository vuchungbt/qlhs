<%
// Đặt các biến mặc định
if (typeof title === 'undefined') title = 'Không tìm thấy trang';
if (typeof isAuthenticated === 'undefined') isAuthenticated = false;
if (typeof userType === 'undefined') userType = null;
%>
<%- include('../partials/header') %>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="mb-4">
                <i class="bi bi-question-circle-fill text-info" style="font-size: 5rem;"></i>
            </div>
            <h1 class="display-4 fw-bold text-primary mb-4">404</h1>
            <h2 class="mb-3">Không tìm thấy trang</h2>
            <p class="lead mb-4 text-muted">
                <%= typeof message !== 'undefined' ? message : 'Trang bạn đang tìm kiếm không tồn tại hoặc đã bị di chuyển.' %>
            </p>
            <div class="d-grid gap-2 d-md-block">
                <a href="javascript:history.back()" class="btn btn-outline-secondary me-2">
                    <i class="bi bi-arrow-left me-1"></i> Quay lại
                </a>
                <a href="/" class="btn btn-primary">
                    <i class="bi bi-house me-1"></i> Trang chủ
                </a>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %> 